Opening client.py to inspect current doppio function and determine where to add triplo.

Editing client.py: insert triple(x) function mirroring double(x) for triplo calculation. Proceeding to apply edit and commit change.

Committing edited client.py to git with Co-authored-by trailer to record change. Running git add and git commit.

client.py updated and committed. Test quick: run client.py with sample inputs to verify both functions behave. Running python checks.

Run results: printed 10 then exit code 0; double('3')=6, triple('3')=9, double('2.5')=5.0, triple(2.5)=7.5. All good. Next step: deliver changes.


