# Riassunto dei task precedenti

Di seguito un breve riepilogo di ciò che è stato fatto nei tre task precedenti (file di log):

- plan-v0-18-0-doppio-41.md
  - Creato client.py alla radice del repository.
  - Implementata funzione CLI per calcolare il doppio di un numero (es. `python client.py 5` -> `10`).

- plan-v0-18-0-rombo-42.md
  - Creato area_rombo.py alla radice del repository.
  - Implementata CLI per calcolare l'area del rombo usando diagonali (--d1 --d2) o lato+angolo (--side --angle). Esempio: `python area_rombo.py --d1 10 --d2 8`.

- plan-v0-18-0-triplo-43.md
  - Ispezionato client.py e aggiunta funzione `triple(x)` per il calcolo del triplo.
  - File client.py modificato e committato (con Co-authored-by trailer). Test eseguiti: double e triple funzionano anche su input stringa/float (es. '3' -> 6/9; 2.5 -> 5.0/7.5).

File creati/modificati si trovano nella radice del workspace: `client.py`, `area_rombo.py`.

Fine del riassunto.
